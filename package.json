{
  "name": "emu-rabbit-portfolio",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vue-tsc -b && vite build && node scripts/build-seo.mjs && node scripts/verify-seo.mjs",
    "build:app": "vue-tsc -b && vite build",
    "generate:social": "python scripts/generate_social_previews.py",
    "verify:seo": "node scripts/verify-seo.mjs",
    "preview": "vite preview"
  },
  "dependencies": {
    "three": "^0.185.1",
    "vue": "^3.5.41"
  },
  "devDependencies": {
    "@types/three": "^0.185.4",
    "@vitejs/plugin-vue": "^6.0.8",
    "typescript": "^5.9.3",
    "vite": "^8.2.1",
    "vue-tsc": "^3.3.9"
  }
}
